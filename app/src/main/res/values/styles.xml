<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bar_blue</item>
        <item name="colorPrimaryDark">@color/bar_blue</item>
        <item name="colorAccent">@color/bar_blue</item>

    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CallLog_Button_Focus" parent="Base.Animation.AppCompat.DropDownUp">
        <item name="android:background">@drawable/buttononfocus</item>
        <item name="android:textColor">@color/button_color_highlight</item>
    </style>

    <style name="CallLog_Button" parent="Base.Animation.AppCompat.DropDownUp">
        <item name="android:background">@drawable/button</item>
        <item name="android:textColor">@color/button_color</item>
    </style>

    <style name="Menu" parent="@style/AlertDialog.AppCompat.Light">
        <!-- 背景透明 -->
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>

    <style name="ActionSheetDialogAnimation" parent="@style/AlertDialog.AppCompat.Light">
        <item name="android:windowEnterAnimation">@anim/up</item>
        <item name="android:windowExitAnimation">@anim/down</item>
    </style>


    <!-- Base application theme. -->
    <style name="AppTop" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bar_blue</item>
        <item name="colorPrimaryDark">@color/bar_blue</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>



    <style name="AppMainTopContent" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionMenuTextColor">@color/black</item>
        <item name="actionMenuTextColor">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/white</item>
        <item name="background">@color/white</item>
    </style>

    <style name="PopMenuColor">
        <item name="android:popupBackground">@color/white</item>
    </style>

    <style name="PopMenuText">
        <item name="android:textColor">@color/button_text_blue_selector</item>
        <item name="android:textSize">17sp</item>
    </style>


    <!-- 标题样式是ActionBar样式的一部分，所以要先定义ActionBar的样式 -->
    <style name="AppMainTop" parent="@style/AppTop">

        <item name="android:itemBackground">@color/white</item>
        <!--<item name="android:actionMenuTextColor">@drawable/button_text_selector</item>-->

        <item name="android:textAppearanceSmallPopupMenu">@style/PopMenuText</item>
        <item name="android:textAppearanceLargePopupMenu">@style/PopMenuText</item>
    </style>
    <!--然后在ActionBar的样式中通过android:titleTextStyle定义标题的样式
        然后通过android：background定义actionBar颜色（不能在上边定义，否则设为整个页面背景色）
    -->

    <style name="CustomActionBar" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/white</item>
        <item name="android:titleTextStyle">@style/ActionBarTitle</item>
    </style>

    <!-- 自定义标题文字样式 -->
    <style name="ActionBarTitle" parent="@android:style/TextAppearance.Holo.Large">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">20sp</item>
    </style>

    <!-- 自定义菜单文字样式 -->
    <style name="ActionBarMenu" parent="@android:style/TextAppearance.Holo.Large">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="ButtonText">
        <item name="android:textColor">#ffffff</item>
        <item name="android:gravity">center</item>
        <!-- 阴影风格,如粗体，斜体 -->
        <!--<item name="android:textStyle">bold</item>-->
        <!-- 阴影颜色,这里可以处理阴影,外发光,描边等效果 -->
        <item name="android:shadowColor">#98F5FF</item>
        <!-- 阴影x偏移 -->
        <item name="android:shadowDx">0</item>
        <!-- 阴影y偏移 -->
        <item name="android:shadowDy">0</item>
        <!-- 阴影半径 -->
        <item name="android:shadowRadius">3</item>
        <!-- 背景 -->
        <item name="android:background">@drawable/button_selector</item>
    </style>






</resources>